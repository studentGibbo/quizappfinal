<!-- Authors: Drew Ambrosino and Owen O'Connell-->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <link rel="stylesheet" href="/styles.css"/>
  <title>NBA Trivia Home</title>
</head>
<body>
  <div class="container">
    <h1>🏀 NBA Trivia Quiz</h1>

    <section class="stats">
      <p>Welcome, <strong><%= user.username %></strong>!</p>
      <p>Your total correct answers: 
        <strong><%= user.stats.correctCount %> point<%= user.stats.correctCount===1?'':'s' %></strong>
      </p>
      <p>Total questions attempted: 
        <strong>
          <%= user.stats.correctCount + user.stats.incorrectCount %>
        </strong>
      </p>
    </section>

    <div class="actions">
      <a href="/quiz" class="btn">Start Quiz</a>
      <a href="/logout" class="btn">Log Out</a>
    </div>

    <h2>🏆 Leaderboard</h2>
    <table class="leaderboard">
      <thead>
        <tr>
          <th>Rank</th>
          <th>User</th>
          <th>Score</th>
          <th>Accuracy</th>
        </tr>
      </thead>
      <tbody>
        <% topPlayers.forEach((u, i) => { %>
          <tr class="<%= u._id.equals(user._id) ? 'you' : '' %>">
            <td><%= i + 1 %></td>
            <td><%= u.username %></td>
            <td><%= u.stats.correctCount %></td>
            <td><%= u.accuracy %> %</td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>